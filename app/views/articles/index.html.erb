<div>
  <% @articles.each do |article| %>
    <%# <div> %>
      <h2>
        <%= link_to article.title, article_path(article), class: 'index-link-title' %>
      </h2>
      <p><%= article.content %></p>
    <%# </div> %>
  <% end %>
</div>
